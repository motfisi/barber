const buttonConfig = () => ({
  MuiButton: {
    styleOverrides: {
      root: {
        borderRadius: 150,
      },
      sizeLarge: {
        fontSize: 20,
        fontWeight: 500,
        padding: '8px 32px',
      },
    },
  },
});

export default buttonConfig;
