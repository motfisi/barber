import { FC } from 'react';

import MainScreen from '@/components/MainScreen';
import Page from '@/components/Page';

const Home: FC = () => {
  return (
    <Page>
      <MainScreen />
    </Page>
  );
};

export default Home;
